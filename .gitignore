node_modules
.vercel
.env
.env.local
*.log
.DS_Store

# Armazenamento local de testes (fallback enquanto não há Vercel KV).
.storage-local.json

# Cópias locais de referência — NÃO incluir no deploy.
# Os PDFs protegidos ficam em api/_arquivos/ (servidos apenas pela API com login).
/aulas/
/materiais/

# Imagens removidas do site (foto do professor já não é usada).
imagens/
.vercel
.env*
